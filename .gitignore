.idea/

.env
settings.json

.claude/.env
.claude/settings.json

node_modules/
bun.lock

history/
thoughts/
!.claude/history/
!.claude/history/.gitkeep

.venv

# Runtime artifacts from cc-upgrade-pai gap-tracker (regenerated on demand)
.claude/skills/cc-upgrade-pai/references/gap-manifest.json

# Test-run baselines (regenerated by test harness)
.test-current.xml

# TDD baselines and mutation testing → global gitignore (~/.gitignore_global)
